		% \State ${
		% 	P
		% 	\gets
		% 	\bigcup_{f\in(\textsc{LCP},\textsc{SJ},\textsc{DL},\textsc{Ours})}
		% 	\arg\operatorname{top}^{(3)}f(x,r)
		% }$
		% \State ${
		% 	\bm{w}\gets(4,3,2,1)
		% }$
		% \State ${
		% 	O
		% 	\gets
		% 	\textsc{WTB}_{r\in R}^{(3)}(
		% 		\textsc{LCP}(x,r), % rank-1 voter
		% 		\textsc{SJ}(x,r), % rank-2 voter
		% 		\textsc{DL}(x,r), % rank-3 voter
		% 		\textsc{Ours}(x,r) % rank-4 voter
		% 	)
		% }$

		% My intuition:
% DL captures insertions, deletions, substitutions, and adjacent transpositions?
% LCS and CH capture ordered and unordered character overlap respectively?
